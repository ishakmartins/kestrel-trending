{
  "name": "kestrel-trending-site",
  "type": "module",
  "private": true,
  "scripts": {
    "stage": "node scripts/stage-dashboard.mjs",
    "dev": "npm run stage && astro dev",
    "build": "npm run stage && astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^5.14.1"
  }
}
