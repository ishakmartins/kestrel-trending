requests>=2.31.0
beautifulsoup4>=4.12.0
pandas>=2.0.0
plotly>=5.18.0
# matplotlib is NOT required by trending/scraper.py or trending/report.py (PNG rendering
# was dropped in the Phase 3 migration). Only needed to run the retired
# notebooks/kestrel-trends_bumpchart-*.ipynb manually: pip install matplotlib>=3.7.0
#
# playwright is NOT required either — add only if trends24.in/indonesia/ table tab
# is confirmed JS-injected (i.e. parse_detail_stats_table returns None despite the
# table being visible in a browser).  If needed: pip install playwright && playwright install chromium
